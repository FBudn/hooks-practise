import React from "react";
import { TextInfo } from "../styles/styles";

// komponent Lazy wpięty do FiringLazyComp komponentu

const LazyComponent = () => {
  return <TextInfo>I am a lazy component </TextInfo>;
};

export default LazyComponent;
